# MNIST-Digit-Classification-Project
 •  Built a deep learning model using the MNIST dataset to classify handwritten digits (0-9). Implemented a  convolutional neural network (CNN) to recognize and accurately predict digit classes, achieving high  performance in image recognition tasks.
